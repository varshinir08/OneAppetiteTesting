package com.cts.mfrp.oneappetite.pages.vendor;

import org.openqa.selenium.WebDriver;

/** Compatibility alias — delegates to {@link VendorMenuPage}. */
public class MenuPage extends VendorMenuPage {
    public MenuPage(WebDriver driver) { super(driver); }
}
